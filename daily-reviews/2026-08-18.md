# Daily Trading Review — 2026-08-18

## 1. Data availability

**`trades.jsonl` does not exist in the working directory.** Per `tradeLogger.ts`, this file is append-only and is only created on the *first* `logTradeEvent()` call (`fs.appendFileSync`) — its total absence means **zero trade events of any kind (entry, rejection, breakeven, trailing, exit) were logged by the agent today.** This isn't a missing-file error to work around; it's itself the headline finding.

`watchlist.json` (generated today, `2026-08-18T23:05:32Z`) selected two symbols:

| Symbol |
|---|
| WEAV |
| DUOT |

## 2. Computed metrics

All zero/undefined, since there is no trade data for the date:

| Metric | Value |
|---|---|
| Entries placed | 0 |
| Entries rejected | 0 |
| Exits (by reason) | 0 (HARD_STOP: 0, BREAKEVEN: 0, TRAILING: 0, EOD: 0) |
| Win rate | N/A (no closed trades) |
| Total realized P&L | $0.00 |
| Avg winner / avg loser | N/A |
| Avg hold time | N/A |

## 3. What worked / what didn't (root causes, from `agent_run.log`, `agent_run.err.log`, and `webull_trade_sdk.log`)

**Didn't work — watchlist/stream mismatch.** `stockScreener.ts` correctly produced today's watchlist (WEAV, DUOT), meeting the stated gap/volume/float criteria. But the live agent (`agent_run.log`) never subscribed to either symbol — it seeded EMA state and subscribed only to **MSTZ**, which isn't on today's watchlist at all:
```
Seeding EMA state from historical bars...
  MSTZ: seeded with 30 historical closes
[stream] subscribed to MSTZ (TICK)
```
This is a wiring gap between the screener's output and whatever symbol list `main.ts` actually fed into the stream subscription — the strategy spent the whole session watching a stock it wasn't supposed to be trading, and never looked at WEAV/DUOT at all.

**Didn't work — market data stream stability.** `agent_run.err.log` shows repeated MQTT failures throughout the session: `ECONNRESET`, `connack timeout`, and even DNS resolution failures (`getaddrinfo ENOTFOUND data-api.sandbox.webull.com`). `agent_run.log` shows the stream reconnecting roughly every 2–8 seconds. With that little uptime per connection, only a handful of bars landed between drops — never enough to sustain a real EMA-9/20 crossover. Every signal reading logged was `NONE`, with a single `FLAT` — no `BUY`/`SELL` signal ever fired.

**Consequence chain.** No crossover signal → `orderManager.ts` never had a reason to call `logTradeEvent()` → `trades.jsonl` was never created. The zero-trades outcome is the direct, expected downstream effect of (a) subscribing to the wrong symbol and (b) a stream too unstable to compute a reliable signal even on the symbol it did watch — not a separate failure.

**Risk item — stale AAPL position, stop not tracked.** At startup the agent reconciled an existing broker position:
```
[risk] reconciled AAPL: qty=2 cost=310.44 phase=HARD_STOP stopTracked=false
```
`stopTracked=false` means this position's hard stop is not being actively managed this session — it sat unmonitored for the full run. This AAPL exposure traces back to manual order-testing earlier this morning (`webull_trade_sdk.log.2026-08-18_08`, ~07:2x–07:55, from ad hoc test scripts, not the EMA agent), which itself hit a string of API errors while probing order placement: overnight-session rejection (`OAUTH_OPENAPI_OVERNIGHT_CAN_NOT_TRADING_FOR_NON_TRADING_HOURS`), a stuck cancel (`ORDER_CAN_NOT_BE_CANCEL_FOR_PENDING_SUBMIT`), an opposing open order conflict (`OPEN_ORDER_HAS_BOX_ORDER`), and a sell-with-no-position error. Worth confirming intentionally before next session — it's live exposure the automated risk logic isn't currently covering.

**Possible auth issue.** Both today's `webull_trade_sdk.log` and the 07:xx session show repeated `_check_token_enable result is False` from the trade SDK client initializer (13:56, 14:04, 14:45 today). This didn't matter today since no order was ever attempted by the agent, but it's worth checking independently — if the token check is genuinely failing, a real entry signal would likely have failed to place regardless.

## Bottom line

Zero trades today, but not because the market didn't offer setups — the agent was structurally prevented from trading: it watched the wrong symbol (MSTZ instead of the screened WEAV/DUOT) over a market-data connection that dropped every few seconds. Two concrete fixes to verify before the next session: (1) confirm `main.ts` is actually reading `watchlist.json`'s current symbols into the stream subscription, and (2) investigate the MQTT reconnect/DNS instability against the sandbox host. Separately, the stale AAPL position with `stopTracked=false` deserves a manual look.
